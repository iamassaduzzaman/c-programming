#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    // read two integer from user
    int a, b;
    scanf("%d %d",&a, &b);
    
    // get the summation of two integers
    int sum = a + b;
    printf("%d\n", sum);
    return 0;
}
