#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    // we can take input with space by using gets() and fgets() function

    /*
        gets() keeps reading input until newline character or end-of-file(EOF) shows up.
         This can lead to buffer overflow as it doesn't check array bounds of variable.
         While in case of fgets() it will also stop when maximum limit of input characters is reached.

         */
    return 0;
}