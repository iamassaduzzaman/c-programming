#include <stdio.h>

int main()
{
  /*
  #syntax of funtion

  return_type name(parameter)
  {
    some code;
    return value;
  }

   4 ways!
   1. return_type + parameter
   2. return_type + no parameter
   3. no return_type + parameter
   4. no return_tyep + no parameter

   two types of function
   1. user define function
   2. built in function (function written by someone else)

  */
  return 0;
}