#include <stdio.h>

int main()
{
  // ceil() - headerfile <math.h>
  // returns the nearest integer greater than the provided argument

  // floor() - headerfile <math.h>
  // used to convert a floating point number to its immediately smaller intege

  // round() - headerfile <math.h>
  // returns the nearest integer value (rounded value) of the given float,
  // integer, or double number based on the decimal part of the number.

  // sqrt() - headerfile <math.h>
  // The sqrt() function takes a single argument (in double ) and
  // returns its square root (also in double )

  // pow() - headerfile <math.h>
  // The pow() function takes 'double' as the argument and returns a 'double' value
  // prototype :  result = pow(base,power);

  // abs() - headerfile <stdlib.h>
  // abs(int x) returns the absolute value of int x
  return 0;
}