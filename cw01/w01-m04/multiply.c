#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{
  long long int A, B, multiplication;
  scanf("%lld %lld", &A, &B);

  multiplication = A * B;
  printf("%lld\n", multiplication);
  return 0;
}